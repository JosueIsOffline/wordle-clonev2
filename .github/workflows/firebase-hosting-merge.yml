
# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Select the environment to deploy'
        required: true
        default: 'PRODUCTION'
        type: choice
        options:
          - PRODUCTION
          - development
          
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    environment: ${{ inputs.environment }}
    steps:
      - name: Check out the code
        uses: actions/checkout@v4 
          
      - name: Cache Node modules
        uses: actions/cache@v4
        with: 
          path: ~/.npm
          key: ${{ runner.os }}-node-${{ hashFiles('package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      - name: Install dependencies and build
        run: npm ci && npm run build

      - name: Set Firebase projectId and channelId
        run: |
          if [ "${{ inputs.environment }}" == "production" ]; then
            echo "PROJECT_ID=your-production-project-id" >> $GITHUB_ENV
            echo "CHANNEL_ID=live" >> $GITHUB_ENV
          else
            echo "PROJECT_ID=your-development-project-id" >> $GITHUB_ENV
            echo "CHANNEL_ID=preview" >> $GITHUB_ENV
          fi

      - name: Deploy to Firebase Hosting
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_WRODLE_CLONEV2_3412_PRODUCTION }}
          channelId: ${{ env.CHANNEL_ID }}
          projectId: ${{ env.PROJECT_ID }}

      - name: Notify on Deployment
        if: success()
        run: echo "Deployment to Firebase Hosting was successful!"
