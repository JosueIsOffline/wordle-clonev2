# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - master
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    environment: production
    steps:
      - uses: actions/checkout@v4
      - name: Install dependencies and build
        run: npm ci && npm run build

      - name: Check Environment Secrets
        run: |
          echo "Verificando los secretos del environment..."
          if [ -z "${{ secrets.FIREBASE_SERVICE_ACCOUNT_WRODLE_CLONEV2_3412_PROD }}" ]; then
            echo "No se encontró el secret FIREBASE_SERVICE_ACCOUNT_WRODLE_CLONEV2_3412_PROD en el environment."
          else
            echo "Se encontró el secret FIREBASE_SERVICE_ACCOUNT_WRODLE_CLONEV2_3412_PROD."
          fi
        
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_WRODLE_CLONEV2_3412 }}
          channelId: live
          projectId: wrodle-clonev2-3412
        env: 
          FIREBASE_TOKEN: ${{ secrets.FIREBASE_TOKEN }}
      
